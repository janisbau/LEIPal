# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=leipal
POSTGRES_USER=leipal
POSTGRES_PASSWORD=changeme

# Derived connection URL (used by app and alembic)
DATABASE_URL=postgresql+psycopg://leipal:changeme@localhost:5432/leipal

# Data storage path (where raw GLEIF files are downloaded)
DATA_DIR=./data
